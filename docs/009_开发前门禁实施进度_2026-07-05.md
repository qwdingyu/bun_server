# 开发前门禁实施进度

> 回答用户问题：`005_开发前文档审查报告_2026-07-05.md` 中的内容是否全部实施完毕，除了 Logto？
>
> 结论：**尚未全部完成**。目前已经补齐认证会话、RBAC 优先级、批量接口、安全默认值、接口契约、前端集成契约和 API 契约测试可信度；但 MySQL schema parity、默认 seed 统一、菜单/按钮权限与审计日志标准模块仍未完成。

## 1. 已完成项

### 1.1 主线文档与过时文档治理

状态：已完成。

证据：

- 保留 `001_后台管理复用框架评估_2026-07-05.md`。
- 保留 `005_开发前文档审查报告_2026-07-05.md`。
- 保留 Logto 评估与轻量认证方案文档 `006/007`。
- 已删除过时的 `docs/curl.md`、`docs/optimization-suggestions.md` 以及早期 `002/003/004` 思路文档。

### 1.2 第一阶段实施规格

状态：已完成第一版并更新。

证据：

- `008_后台脚手架第一阶段实施规格_2026-07-05.md`。

### 1.3 AuthProvider/AuthContext seam

状态：已完成第一版。

证据：

- `src/modules/auth/auth-context.js`
- `src/modules/auth/providers/local-jwt.js`
- `src/modules/auth/index.js`
- `src/middleware/auth.js`
- `tests/test-auth-provider.js`

验证：

```text
bun run tests/test-auth-provider.js
```

### 1.4 refresh token 服务端会话闭环

状态：已完成第一版。

证据：

- `src/models/SessionModel.js`
- `src/controllers/UserController.js`
- `src/utils/jwt/index.js`
- `src/models/schema/sqlite.js`
- `tests/test-session-model.js`
- `tests/test-auth-session-flow.js`

实现规则：

- 登录/注册创建 `user_sessions`。
- refresh token 只存 hash。
- refresh token 刷新时校验服务端会话。
- 刷新成功轮换 refresh token。
- 登出可按 refresh token 撤销会话。

验证：

```text
bun run tests/test-session-model.js
bun run tests/test-auth-session-flow.js
```

### 1.5 SQLite DDL 初始化根因修复

状态：已完成。

证据：

- `src/db/index.js` 改为执行完整 SQLite DDL，避免按分号拆分导致注释块和建表顺序问题。

验证：

```text
bun -e "import './src/main.js'; console.log('main import ok')"
```

### 1.6 用户认证查询与基础查询修复

状态：已完成。

证据：

- `src/models/UserModel.js` 中 `authenticate()` 使用 Drizzle `or(eq(username), eq(email))`。
- `src/models/BaseModel.js` 中 `buildWhereClause()` 使用 schema column + `eq/isNull/like`。

### 1.7 RBAC 优先级

状态：已完成第一版。

证据：

- `src/models/PermissionModel.js`
- `tests/test-rbac-priority.js`

当前规则：

```text
1. 角色类接口可通过 requireRoles 对 super_admin 放行。
2. user_permissions 显式 deny 命中则拒绝。
3. user_permissions 显式 grant 命中则通过。
4. role_permissions 命中则通过。
5. 默认拒绝。
```

验证：

```text
bun run tests/test-rbac-priority.js
```

### 1.8 半成品批量用户接口

状态：已完成。

证据：

- `src/routes/users.js` 已移除 `NOT_IMPLEMENTED` 返回。
- `src/controllers/UserController.js` 新增 `batchUpdateStatus()` 与 `batchDeleteUsers()`。
- `src/models/UserModel.js` 新增 `batchUpdateStatus()` 与 `batchSoftDeleteUsers()`。
- `tests/test-user-batch-routes.js` 验证批量静态路由不会被 `/:id` 抢匹配。

规则：

- 仅 `super_admin` 可访问。
- 不能操作当前登录用户自己。
- 批量删除使用软删除。

踩坑记录：

- 背景：`DELETE /batch` 与 `DELETE /:id` 同属用户路由。
- 原因：动态路由若先注册，容易抢先匹配静态批量路由。
- 排查过程：新增批量接口测试后发现响应状态不符合预期，逐步读取响应定位到路由和校验链路。
- 解决方案：将 `/batch/update-status` 与 `/batch` 放到 `/:id` 前面注册，并新增专项测试固定行为。
- 建议：后台脚手架中所有静态管理路由都应前置于动态参数路由。

附加修正：

- `src/middleware/validation/index.js` 中 JSON Schema `integer` 现在接受 JS `number` 且要求 `Number.isInteger()`，避免 JSON 整数被误判为类型错误。

### 1.9 安全默认值

状态：已完成第一版。

证据：

- `config.yaml` 不再默认 `origin: '*' + credentials: true`。
- `config.yaml` 默认 CSP 不再包含 `unsafe-inline`。
- `src/utils/config.js` 支持生产环境 JWT secret/CORS 校验。
- `src/utils/jwt/index.js` 生产环境遇到占位 JWT secret 会拒绝启动。
- `src/middleware/security/index.js` 修正 CORS credentials 默认逻辑。

验证：

```text
bun -e "import { loadConfig, validateConfig } from './src/utils/config.js'; const config = loadConfig(); console.log(validateConfig(config));"
```

### 1.10 后台接口契约

状态：已完成第一版。

证据：

- `010_后台接口契约_2026-07-05.md`

覆盖：

- 成功响应。
- 错误响应。
- 分页响应。
- 注册、登录、刷新、登出、当前用户。
- 用户 CRUD 与批量接口。
- RBAC 与安全默认值。
- MySQL 当前支持级别说明。

### 1.11 前端集成契约

状态：已完成第一版。

证据：

- `011_后台前端集成契约_2026-07-05.md`

覆盖：

- 前端认证状态模型。
- token 存储策略。
- 登录、刷新、登出流程。
- 请求拦截器与刷新单飞机制。
- 路由守卫。
- 菜单/按钮权限后续数据结构。
- 统一错误处理。
- 与未来 Logto 的兼容边界。

### 1.12 API 测试可信度

状态：已修正当前主线测试。

证据：

- `tests/test-api.js` 已按 `010_后台接口契约_2026-07-05.md` 重写，只测试当前真实暴露接口。
- 测试失败时会 `process.exit(1)`，不再出现内部断言失败但 `run-tests.js` 误判通过的问题。

验证：

```text
bun run tests/test-api.js
bun run tests/run-tests.js
```

当前结果：两条命令均通过，`run-tests.js` 不再掩盖旧 API 契约失败。

## 2. 尚未完成项

### 2.1 MySQL schema parity

状态：未完成。

当前事实：

- SQLite schema 覆盖 `users/system_config/roles/permissions/user_roles/role_permissions/user_permissions/user_sessions`。
- MySQL schema 当前只覆盖 `users/system_config`。

当前建议：

- 第一阶段文档声明 MySQL 为实验支持。
- 若近期要用于 MySQL 项目，必须先补齐 schema parity 和测试矩阵。

### 2.2 默认角色与权限 seed

状态：未完成。

需要补充：

- `super_admin/admin/user` 默认角色。
- `users:list/read/create/update/delete` 等标准权限。
- seed 脚本幂等性。
- 测试环境与生产初始化策略分离。

### 2.3 前端菜单/按钮权限与审计日志标准模块

状态：未完成。

说明：这是后台管理复用框架的重要能力，但不是当前本地认证安全闭环的最小前置条件。

## 3. 当前不能宣称完成的原因

除 Logto 外，`005` 中仍至少有以下缺口：

- MySQL 支持级别只完成文档声明，未完成 schema parity。
- 默认角色/权限 seed 未统一。
- 菜单/按钮权限与审计日志仍处于后续规划。
- 旧测试数据和初始化脚本仍有重复与历史包袱，虽然当前 API 契约测试已修正，但测试体系仍需继续瘦身和去重。

### 3.1 全量测试可信度踩坑

- 背景：运行 `bun run tests/run-tests.js` 时最终显示“所有测试通过”。
- 原因：部分旧测试脚本内部记录失败但没有以非 0 状态退出，导致启动器误判。
- 排查过程：读取全量测试输出后发现 `test-api.js` 多处断言失败，但脚本进程仍被 `run-tests.js` 识别为成功。
- 解决方案：已按 `010_后台接口契约_2026-07-05.md` 重写 `tests/test-api.js`，只验证当前真实接口，并在失败时退出非 0。
- 建议：后续继续整理 `tests/init-db.js` 的重复结构，避免 schema/seed 逻辑长期双写。

## 4. 建议下一步

1. 新增 `012_RBAC权限模型与种子数据_2026-07-05.md`。
2. 决定 MySQL 是继续实验声明，还是立即补 schema parity。
3. 整理 `tests/init-db.js` 与全量测试数据隔离，提升全量测试可维护性。
4. 规划菜单/按钮权限与审计日志标准模块。
